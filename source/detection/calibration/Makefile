# ============================================================
# PDM Detection Makefile
# ============================================================

CC := gcc

# ------------------------------------------------------------
# Common flags
# ------------------------------------------------------------
COMMON_CFLAGS := -fPIC \
                 -fno-stack-protector \
                 -fno-builtin \
                 -fno-jump-tables \
                 -fno-common \
                 -pthread

# ------------------------------------------------------------
# Targets
# ------------------------------------------------------------
CALIBRATION := calibration

.PHONY: all clean

all: $(CALIBRATION)


# ============================================================
# calibration (cache latency calibration tool)
# ============================================================
$(CALIBRATION): calibration.c
	$(CC) -O2 -march=native -o $@ $<


# ============================================================
# Cleanup
# ============================================================
clean:
	rm -f $(CALIBRATION)


